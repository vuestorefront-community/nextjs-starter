{
  "name": "middleware",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev src/index.ts",
    "build": "echo 'Add build script here'",
    "test": "echo 'Add test script here'",
    "lint": "echo 'Add lint script here'"
  },
  "dependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "@vsf-enterprise/sapcc-api": "^10.0.0",
    "@vsf-enterprise/sapcc-types": "^4.0.0",
    "@vsf-enterprise/unified-api-sapcc": "^5.0.0",
    "@vue-storefront/middleware": "^5.4.0",
    "consola": "^3.2.3",
    "ts-node-dev": "^2.0.0"
  }
}
