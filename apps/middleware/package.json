{
  "name": "middleware",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev src/index.ts",
    "build": "echo 'Add build script here'",
    "test": "echo 'Add test script here'",
    "lint": "echo 'Add lint script here'"
  },
  "dependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "@vsf-enterprise/unified-api-sapcc": "^0.20.0",
    "@vsf-enterprise/sapcc-api": "^5.0.0",
    "@vue-storefront/middleware": "^3.9.0",
    "consola": "^3.2.3",
    "ts-node-dev": "^2.0.0"
  }
}
